# TPM 프로젝트 설정

## tpm 플러그인 라우팅 힌트

이 프로젝트는 tpm(Theater Production Manager) 플러그인을 사용합니다.

### 주요 명령
- `/tpm:propose` — 공연 기획 제안서 자동 생성 (전 과정 오케스트레이션)
- `/tpm:help` — 전체 명령 안내
- `/tpm:setup` — 초기 설정

### 자동 라우팅 키워드
다음 키워드가 포함된 요청은 tpm 플러그인으로 자동 라우팅됩니다:
- 공연 기획, 뮤지컬, 연극, 공연 제안서
- 예산 계획, 마케팅 전략, 시장 분석
- 포스터 이미지, 컨셉 이미지 생성

---

# 마크다운 작성 가이드
- 문서 작성 시 명사체(명사형 종결어미) 사용
  - 예시: "~한다" → "~함", "~이다" → "~임", "~된다" → "~됨"
  - 예시: "지원한다" → "지원", "사용할 수 있다" → "사용 가능"
- 한 줄은 120자 이내로 작성, 긴 문장은 적절히 줄바꿈
- 빈 줄(`\n\n`) 없이 줄바꿈하는 모든 경우, 줄 끝에 스페이스 2개 필수 (없으면 렌더링 시 한 줄로 합쳐짐)
- 간결하고 객관적인 기술 문서 스타일 유지

---

## 진행 상황

<!-- propose 스킬이 각 Phase 완료 시 자동 업데이트 -->

---